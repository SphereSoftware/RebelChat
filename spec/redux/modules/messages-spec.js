'use babel';

import {
  MessageObject,
} from '../../../lib/objects'

import messages, {
  setAllMessages,
  addMessages,
  sendMessage,
  receiveMessage,
  updateMessage,
  replaceMessage,
} from '../../../lib/redux/modules/messages'


describe('messages reducer', () => {
})
